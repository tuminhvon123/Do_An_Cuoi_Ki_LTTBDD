<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".AppFoodApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppFood">

        <activity
            android:name=".presentation.ui.activity.AddFoodActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ui.activity.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.activity.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.activity.DetailActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.activity.CartActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.activity.CheckoutActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.ui.activity.LoginActivity"
            android:exported="true" />

        <activity
            android:name=".presentation.ui.activity.OrderHistoryActivity"
            android:exported="false" />
    </application>

</manifest>